// Do something with the submitted data (e.g., save it to a database)
    async function handleScraping(index) {
        const start_date = "Hello " + index;
        return (start_date);
    }

    handleScraping();